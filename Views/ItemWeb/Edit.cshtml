@using InvenScan.Entity
@model Item
@{ ViewData["Title"] = "Edit Item"; }

<div class="page-header">
    <div class="d-flex align-items-center gap-3">
        <a asp-controller="ItemWeb" asp-action="Index" class="btn btn-sm btn-outline-secondary">
            <i class="bi bi-arrow-left"></i>
        </a>
        <h1 class="page-title mb-0">Edit Item</h1>
    </div>
</div>

<div style="max-width:560px">
    <div class="card p-4">
        @if (ViewData["Error"] != null)
        {
            <div class="alert-is alert-is-danger mb-3">
                <i class="bi bi-exclamation-triangle-fill"></i>@ViewData["Error"]
            </div>
        }
        <form asp-controller="ItemWeb" asp-action="Edit" asp-route-id="@Model.Id" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-3">
                <label class="form-label">Item Code <span style="color:var(--danger)">*</span></label>
                <input name="ItemCode" class="form-control" value="@Model.ItemCode" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Item Name <span style="color:var(--danger)">*</span></label>
                <input name="ItemName" class="form-control" value="@Model.ItemName" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Description</label>
                <textarea name="Description" class="form-control" rows="2">@Model.Description</textarea>
            </div>
            <div class="row">
                <div class="col-6 mb-3">
                    <label class="form-label">Unit</label>
                    <input name="Unit" class="form-control" value="@Model.Unit" />
                </div>
                <div class="col-6 mb-3">
                    <label class="form-label">Min Stock</label>
                    <input name="MinStock" type="number" class="form-control" value="@Model.MinStock" min="0" />
                </div>
            </div>
            <button type="submit" class="btn btn-primary w-100">
                <i class="bi bi-save me-1"></i>Update Item
            </button>
        </form>
    </div>
</div>
